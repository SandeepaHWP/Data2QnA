name: Tests

on:
  workflow_dispatch:

jobs:
  test-one:
    runs-on: ubuntu-latest
    steps:
      - name: Dummy test one
        run: |
          echo "Running test one..."
          sleep 2
          echo "Test one passed"

  test-two:
    runs-on: ubuntu-latest
    steps:
      - name: Dummy test two
        run: |
          echo "Running test two..."
          sleep 2
          echo "Test two passed"

  test-three:
    runs-on: ubuntu-latest
    steps:
      - name: Dummy test three
        run: |
          echo "Running test three..."
          sleep 2
          echo "Test three passed"
